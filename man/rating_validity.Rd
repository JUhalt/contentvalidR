% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_validity.R
\name{rating_validity}
\alias{rating_validity}
\title{Analyze a Hinkin-Tracey construct-rating content-validity pretest}
\usage{
rating_validity(
  ratings,
  item_col = "item",
  rater_col = "rater",
  construct_col = "construct",
  rating_col = "rating",
  target_map = NULL,
  target_col = "target_construct",
  scale_min = 1,
  scale_max = 5,
  alpha = 0.05,
  adjust = c("none", "holm"),
  orbiting_r = NULL,
  judge_type = c("naive", "expert")
)
}
\arguments{
\item{ratings}{Long-format rating data.}

\item{item_col, rater_col, construct_col, rating_col}{Column names.}

\item{target_map}{Optional named item-to-target mapping.}

\item{target_col}{Target column used when \code{target_map} is \code{NULL}.}

\item{scale_min, scale_max}{Endpoints of the equally spaced integer rating
scale.}

\item{alpha}{Significance level for item-level inferential screening.}

\item{adjust}{Planned-contrast p-value adjustment: \code{"none"} (historical
planned-comparison logic) or \code{"holm"}.}

\item{orbiting_r}{Optional average focal-orbiting correlation. For multiple
target scales, use a named numeric vector keyed by target.}

\item{judge_type}{Either \code{"naive"} or \code{"expert"}. Colquitt normative labels
are not applied to expert-judge data.}
}
\value{
An object of class \code{contentvalid_rating} containing item-level
\code{results}, \code{scale_summary}, full planned \code{contrasts}, settings, and design
information.
}
\description{
Provides the recommended user-facing workflow for a fully crossed
construct-rating study. Judges rate each item against its intended construct
definition and one or more orbiting definitions. \code{rating_validity()} combines:
\itemize{
\item Hinkin-Tracey correspondence (HTC),
\item Hinkin-Tracey distinctiveness (HTD),
\item one-way repeated-measures ANOVA (with Greenhouse-Geisser correction) for each item, and
\item planned paired target-versus-orbiting contrasts.
}

Item-level output is diagnostic rather than a coefficient dump: it identifies
the strongest competing construct, describes why an item was flagged, and
labels statistical screening decisions \code{"Retain"}, \code{"Review"}, or
\code{"Insufficient data"}. \code{"Review"} is not an instruction to delete the item.

Colquitt et al. (2019) norms are applied only to \strong{target-scale averages} of
HTC and HTD, matching the level at which those empirical benchmarks were
constructed. The labels are suppressed for expert judges.
}
\examples{
set.seed(12)
d <- expand.grid(item = c("A1", "A2", "B1"), rater = 1:20,
                 construct = c("A", "B", "C"))
d$target_construct <- ifelse(d$item == "B1", "B", "A")
d$rating <- ifelse(d$construct == d$target_construct,
                   pmin(5, pmax(1, round(rnorm(nrow(d), 4.5, .6)))),
                   pmin(5, pmax(1, round(rnorm(nrow(d), 2.0, .7)))))
fit <- rating_validity(d, scale_min = 1, scale_max = 5)
fit
summary(fit)
}
\references{
Hinkin, T. R., & Tracey, J. B. (1999). An analysis of variance approach to
content validation. \emph{Organizational Research Methods, 2}(2), 175-186.

Colquitt, J. A., Sabey, T. B., Rodell, J. B., & Hill, E. T. (2019).
Content validation guidelines: Evaluation criteria for definitional
correspondence and definitional distinctiveness. \emph{Journal of Applied
Psychology, 104}(10), 1243-1265. \doi{10.1037/apl0000406}
}
