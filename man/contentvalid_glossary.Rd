% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glossary.R
\name{contentvalid_glossary}
\alias{contentvalid_glossary}
\title{Glossary of contentvalidR indices and status terms}
\usage{
contentvalid_glossary(workflow = NULL)
}
\arguments{
\item{workflow}{Optionally restrict to one workflow: \code{"item-sort"},
\code{"construct-rating"}, \code{"expert-panel"}, \code{"judge-heterogeneity"}, or
\code{"domain-coverage"}.}
}
\value{
An object of class \code{contentvalid_glossary}: a data frame of \code{term},
\code{workflow}, \code{label}, \code{definition}, and \code{range}, carrying the status
definitions as the \code{"statuses"} attribute.
}
\description{
Plain-language definitions of every abbreviated quantity the package reports,
and of the status labels shared by all flagship workflows.

The same definitions are printed beneath workflow output, so what you read
here is what appears alongside your results. Set
\code{options(contentvalidR.show_key = FALSE)} to suppress those inline keys once
the terms are familiar.
}
\section{A note on benchmark labels}{

Strength labels such as \code{Strong} or \code{Weak} from \code{\link[=interpret_colquitt]{interpret_colquitt()}} are
percentile positions relative to scales published in the measurement
literature. They are not absolute judgements, and they are not comparable
across indices: HTC and HTD sit on different scales with different typical
values, so an HTC of 0.83 can be labelled \code{Weak} in the same analysis where
an HTD of 0.44 is labelled \verb{Very Strong}. Compare each index against its own
benchmark, never against another index's number.
}

\examples{
contentvalid_glossary()
contentvalid_glossary("item-sort")
}
\seealso{
\code{\link[=interpret_colquitt]{interpret_colquitt()}} for the benchmark bands themselves.
}
