% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_handoff.R
\name{content_handoff}
\alias{content_handoff}
\title{Carry content-validity decisions into empirical validation}
\usage{
content_handoff(
  fit,
  keep = "Supported",
  round = 1,
  reverse_keyed = NULL,
  response_scale = NULL
)
}
\arguments{
\item{fit}{A fitted \code{contentvalid_sort}, \code{contentvalid_rating},
\code{contentvalid_expert}, or \code{contentvalid_delphi} object.}

\item{keep}{Statuses that travel forward, defaulting to \code{"Supported"}. Any of
\code{"Supported"}, \code{"Review"}, \code{"Insufficient data"}, or \code{"Descriptive only"}.}

\item{round}{Pretest round this analysis represents. One fit is one round, so
this defaults to \code{1} and matters only when stacking rounds by hand. It
cannot be set for a Delphi fit, which dates each item by the round it
settled in.}

\item{reverse_keyed}{Names of the reverse-worded items, \code{character(0)} if
none is, or \code{NULL} (the default) to leave keying unrecorded. See
"Instrument metadata".}

\item{response_scale}{The lowest and highest answer respondents can give,
such as \code{c(1, 5)}, or \code{NULL} (the default) to leave it unrecorded. This is
the scale of the instrument, not the scale the panel rated on.}
}
\value{
An object of class \code{contentvalid_handoff}, \code{cv_handoff}, and \code{list},
as described under "Object shape".
}
\description{
Packages the item decisions from a finished content-validity workflow so they
can be carried into an empirical scale-development workflow without retyping
item names or losing the record of why each item was kept.

The result holds the item names that survived content review, the construct
each belongs to where the design defines one, a per-item evidence table, the
statistics behind each decision, and the provenance of the analysis. It is
plain data, so a downstream package can read it without contentvalidR being
installed.
}
\details{
Item-level workflows are accepted: \code{\link[=sort_validity]{sort_validity()}}, \code{\link[=rating_validity]{rating_validity()}},
\code{\link[=expert_validity]{expert_validity()}}, and \code{\link[=delphi_validity]{delphi_validity()}}. \code{\link[=judge_validity]{judge_validity()}} and
\code{\link[=domain_validity]{domain_validity()}} are refused, because their rows are judges and blueprint
cells rather than items, so there is no item set to carry forward.

Items that do not meet \code{keep} are not dropped from the record. They stay in
\code{item_evidence} with \code{carried = FALSE}, so a reader can see what was held
back and why. Review is not deletion.
}
\section{Object shape (schema version 1)}{

The object has class \code{c("contentvalid_handoff", "cv_handoff", "list")}. A
consumer matches on \code{"cv_handoff"} and reads these fields:

\describe{
\item{\code{items}}{character vector of the carried item names, that is
\code{item_evidence$item[item_evidence$carried]}, unique and in results order.}
\item{\code{scales}}{named list mapping each construct to its carried items, or
\code{NULL} when the design has no construct mapping. Expert relevance and
essentiality rate a single item set with no construct column, so they
produce \code{NULL}. Membership is one to one.}
\item{\code{item_evidence}}{data frame with one row per reviewed item: \code{item},
\code{scale} (\code{NA} without a construct mapping), \code{carried}, \code{status},
\code{recommendation}, \code{n_judges}, \code{rule}, and \code{round}. For a Delphi handoff
\code{round} differs between items; see "A Delphi handoff". From
contentvalidR 0.7.0 it also carries \code{keying}, \code{response_min}, and
\code{response_max}, described under "Instrument metadata".}
\item{\code{item_statistics}}{data frame, one row per item per statistic:
\code{item}, \code{statistic}, \code{value}, \code{criterion} (\code{NA} when the method sets
no explicit criterion), and \code{round}. Which statistics carry a criterion
depends on the workflow rather than on the statistic alone: modified
kappa carries 0.74 from \code{\link[=expert_validity]{expert_validity()}}, and none from a Delphi
handoff, which decides on the consensus threshold. From contentvalidR
0.7.0 it also carries \code{note}, described under "The note column". From
contentvalidR 0.5.0 it also
carries \code{lower}, \code{upper}, \code{interval_method}, and \code{interval_level},
described under "Intervals".}
\item{\code{provenance}}{list with \code{schema_version}, \code{package},
\code{package_version}, \code{workflow}, \code{mode}, \code{keep}, \code{method}, \code{citation},
\code{settings}, \code{design}, and \code{created}.}
\item{\code{panel_statistics}}{added in contentvalidR 0.5.0. Data frame of
panel-level statistics, with the same columns as \code{item_statistics} less
\code{item}, including \code{note} from 0.7.0. It holds the panel agreement
coefficient when \code{\link[=expert_validity]{expert_validity()}} computed one, and has zero rows
otherwise.}
}

This shape is agreed with the \code{nomologR} package, which consumes it in
\code{nomo_screen()} and \code{nomo_run()}. Neither package depends on the other.
Fields and columns added within schema version 1 are optional for a reader,
which should check that they are present rather than assume it.
}

\section{What version 1 freezes}{

Schema version 1 is frozen as of contentvalidR 0.7.0. Code that reads a
handoff can rely on all of the following, in every release that reports
\code{schema_version = 1}:
\itemize{
\item The six top-level fields above, under those names.
\item In \code{item_evidence}: \code{item}, \code{scale}, \code{carried}, \code{status},
\code{recommendation}, \code{n_judges}, \code{rule}, \code{round}, \code{keying}, \code{response_min},
\code{response_max}.
\item In \code{item_statistics}: \code{item}, \code{statistic}, \code{value}, \code{criterion}, \code{round},
\code{lower}, \code{upper}, \code{interval_method}, \code{interval_level}, \code{note}.
\item In \code{panel_statistics}: the same columns less \code{item}.
\item In \code{provenance}: \code{schema_version}, \code{package}, \code{package_version},
\code{workflow}, \code{mode}, \code{keep}, \code{method}, \code{citation}, \code{settings}, \code{design},
\code{created}.
}

Each of those columns keeps its name, its position, and its type. Every
handoff carries every column, including when a workflow has nothing to put
in one: a statistic with no interval carries \code{NA} in the four interval
columns rather than dropping them, and a workflow with no panel coefficient
returns a zero-row \code{panel_statistics} with the full set of columns. A reader
can therefore bind handoffs from different workflows without reconciling
their columns.

These are deliberately \strong{not} frozen, and a reader should not depend on
them:
\itemize{
\item The set of rows. Which items, which statistics, and how many of each
depend on the workflow and on the data.
\item The values in the \code{statistic} column. They are labels for display, and may
be reworded in a minor release; match on the workflow in \code{provenance}
instead.
\item The text in \code{note}, \code{rule}, \code{recommendation}, and \code{citation}, which is
prose for a human reader.
\item The contents of \code{settings} and \code{design}, which mirror the fitted object
and grow with it.
}

Neither is the printed output part of the schema. \code{print()} on a handoff is
written for a person, and its layout and wording may change in any release.
Read the fields.

New optional fields and columns may still be added within version 1, at the
end of a data frame or list. A reader written against this section keeps
working when that happens, provided it addresses columns by name.
}

\section{If the schema ever changes}{

Renaming a field, removing one, changing a type, or changing what a field
means is a version 2 change, not a minor release. It would raise
\code{provenance$schema_version} to \code{2L}, and version 1 would keep being
produced for at least one full release cycle so that readers have a
version to fall back on. The release notes would say what moved.

A reader should gate on the version rather than on the contentvalidR
version:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (!inherits(h, "cv_handoff") || h$provenance$schema_version != 1L) \{
  stop("this reader understands handoff schema version 1 only")
\}
}\if{html}{\out{</div>}}

No version 2 is planned.
}

\section{The note column}{

Added in contentvalidR 0.7.0 to \code{item_statistics} and \code{panel_statistics}.
It says why a value or interval is absent or degenerate, in the producing
function's own words, so a reader need not re-derive method-specific
semantics. For example, a Delphi stability row may carry "Kappa is
undefined: every rating fell in the same category in both rounds."

Its contract, agreed with the \code{nomologR} maintainers:
\itemize{
\item It is \strong{display text only}. Never match on it, branch on it, or parse
it. Its wording may change in any minor release without a schema change.
\item It is always a character vector with \strong{no \code{NA}}. \code{""} means there is
nothing to say, not that something is missing, so a row can carry a value
and an empty note.
\item It \strong{never replaces the values}. Whether a statistic is undefined, and
which of the two cases applies, stays readable from \code{value} and
\verb{proportion unchanged} as described under "When a stability statistic is
NA". That inference is the supported way to decide anything.
\item Objects from contentvalidR 0.6.0 and earlier have no such column, and a
reader should treat its absence as every note being empty.
}
}

\section{Intervals}{

Each statistic's interval travels with it, so a reader can tell a unanimous
four-judge panel from a unanimous twenty-judge one. \code{lower} and \code{upper} are
the bounds, \code{interval_method} names the method, and \code{interval_level} is the
confidence level, for example \code{0.95}.
\itemize{
\item Aiken's V: the Penfield-Giacobbi score interval.
\item I-CVI and Psa: the method chosen with \code{proportion_ci}, the Wilson score
interval by default.
\item Panel agreement: the item-resampling percentile bootstrap of
\code{\link[=panel_agreement]{panel_agreement()}}.
}

The four columns are \code{NA} together when a statistic has no interval. That
happens when the method defines none (Csv, HTC, HTD, CVR, the essential
count, modified kappa, IOC, and p-values), when intervals were switched off
with \code{proportion_ci = "none"}, or when the statistic itself could not be
computed. \code{NA} there never stands for missing data.

The handoff reports intervals only. It does not turn them into priors or
weights for a later analysis; that is a question for the consuming package.
}

\section{A Delphi handoff}{

A Delphi study settles one item at a time: an item that reached consensus
early was set aside, and its last round came before the study's final round.
A Delphi handoff therefore carries each item's evidence \strong{from its own last
round}, and \code{round} holds that round's index rather than one constant. It is
the only workflow where \code{round} varies within a handoff.

Each item carries the relevance evidence of its last round, taken from that
round's \code{\link[=expert_validity]{expert_validity()}} fit, with intervals: \code{I-CVI} against the
consensus threshold, \verb{Aiken's V}, and \verb{modified kappa}. Modified kappa
carries no criterion here, because a Delphi decides on the consensus
threshold rather than on the 0.74 rule that \code{\link[=expert_validity]{expert_validity()}} applies.

Two more statistics record whether the panel had stopped moving:
\verb{proportion unchanged}, and the stability statistic that ran, named for its
method, such as \verb{weighted kappa (quadratic)} or \verb{Goodman-Kruskal lambda}.
The chi-square methods add \verb{stability p_value} against \code{alpha}. Stability
travels as evidence beside the decision; it never decides what is carried,
exactly as it never sets an item's status in \code{\link[=delphi_validity]{delphi_validity()}}.
}

\section{When a stability statistic is NA}{

A stability row is always present for a carried item, so an \code{NA} there is a
statement about the data rather than a missing record. There are two cases,
and they can be told apart from the object alone:

\describe{
\item{\verb{proportion unchanged} is also \code{NA}}{The item has no pair of
consecutive rounds: it was rated in one round only, so there was nothing
to compare.}
\item{\verb{proportion unchanged} has a value}{A pair exists, but the
statistic is undefined for that data.}
}

In the second case, \strong{read \verb{proportion unchanged}}, which is often the more
informative number. An undefined kappa beside \verb{proportion unchanged} of 1 is
perfect stability that kappa cannot express: kappa is chance-corrected, and
when every paired rating in both rounds falls in one category, the
disagreement expected by chance is zero, so kappa is 0/0. Reporting only
"not estimable" there would describe a defect that does not exist. Goodman-
Kruskal lambda is undefined when the later round is unanimous, and the
chi-square methods are undefined for a table with fewer than two occupied
rows or columns.

\code{delphi_validity()} states the reason in \code{details$stability$note}, and from
contentvalidR 0.7.0 the handoff carries that sentence in the \code{note} column
of \code{item_statistics}, described under "The note column".
}

\section{Instrument metadata}{

Added in contentvalidR 0.7.0, at the request of the \code{nomologR} maintainers,
because two empirical computations cannot be done correctly without them.
\itemize{
\item \code{keying} is \code{1} for a forward-worded item, \code{-1} for a reverse-worded one,
and \code{NA} when nobody said. An even-odd consistency index must recode
reverse-worded items before splitting the scale, or a perfectly consistent
respondent looks careless. And a negative corrected item-total correlation
means opposite things in the two cases: on an item that was never recoded
it is a coding error, and on a correctly coded item it is evidence against
the item.
\item \code{response_min} and \code{response_max} are the lowest and highest answers a
respondent can give. Screening for out-of-range answers needs the scale's
limits rather than the observed ones, because a category nobody used is
still a legal answer, and long-string and within-person variability
indices mean different things on a two-point and a seven-point scale.
}

\strong{Both come from the analyst, never from the fit.} A content-validity panel
rates relevance or correspondence on its own scale, usually 1 to 4, which is
not the scale respondents will answer the items on. Copying a fit's \code{lo} and
\code{hi} into \code{response_min} and \code{response_max} would give a downstream reader
the wrong limits, and it would then reject every legitimate top-category
answer. So both default to \code{NA}, which means \emph{unknown}, and a reader should
treat \code{NA} that way rather than assume a forward-worded item or an observed
range.

Supplying \code{reverse_keyed} is a statement about every item: the ones named
are reverse-worded and the rest are not. Pass \code{character(0)} to record that
you checked and none is. So \code{keying} is either \code{NA} for every item or for
none of them, and the same holds for the response scale.

Naming a reverse-worded item without \code{response_scale} gives a warning,
because such an item cannot be recoded without the scale's limits. A reader
that recodes would otherwise have to refuse later, where the problem is
harder to fix.
}

\section{What a handoff does and does not establish}{

Surviving content review is evidence about relevance, representation, and
expert judgment. It does not establish that an item will behave well
empirically. An item can be clearly relevant and still correlate poorly with
its construct or load on an unintended factor. That is what the downstream
empirical analysis tests, which is why the item set travels with its
evidence rather than as a bare list of names.
}

\examples{
relevance <- matrix(
  c(4,4,4,3, 4,4,3,4, 3,4,4,4, 2,2,1,2),
  nrow = 4,
  dimnames = list(NULL, paste0("Item", 1:4))
)
fit <- expert_validity(relevance, mode = "relevance", lo = 1, hi = 4,
                       agreement = "none")
handoff <- content_handoff(fit)
handoff
handoff$items
handoff$item_evidence
handoff$item_statistics

# Carry items flagged for review as well, when the study protocol says so.
content_handoff(fit, keep = c("Supported", "Review"))$items
}
\seealso{
\code{\link[=as.data.frame.contentvalid_workflow]{as.data.frame.contentvalid_workflow()}} for the full results table,
and \code{\link[=content_report]{content_report()}} for manuscript tables.
}
