% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{as.data.frame.contentvalid_workflow}
\alias{as.data.frame.contentvalid_workflow}
\title{Extract workflow results as a plain data frame}
\usage{
\method{as.data.frame}{contentvalid_workflow}(
  x,
  row.names = NULL,
  optional = FALSE,
  component = c("results", "scale_summary"),
  include_interpretation = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted \code{contentvalid_workflow} object.}

\item{row.names, optional}{Present for compatibility with the generic.}

\item{component}{Which component to return: \code{"results"} (the default, one row
per unit of analysis) or \code{"scale_summary"}.}

\item{include_interpretation}{Keep the per-unit interpretation text. It is
informative but long, so set \code{FALSE} for compact tables.}

\item{...}{Ignored.}
}
\value{
A data frame.
}
\description{
Returns a fitted workflow's results as an ordinary data frame, so results can
be filtered, joined, or written out without scraping printed output.

A \code{workflow} column is prepended so that tables from several analyses can be
stacked and stay identifiable.
}
\section{Filtering is your decision, not the package's}{

There is deliberately no helper that returns "the items that passed."
Selecting on \code{status == "Supported"} is a substantive decision that should
appear in your own code where a reader can see it, and \code{Review} never means
an item must be dropped. Keeping the filter explicit keeps that judgement
visible in the analysis script and in the manuscript.
}

\examples{
sorts <- read.csv(
  system.file("extdata", "sort_example.csv", package = "contentvalidR"),
  stringsAsFactors = FALSE
)
fit <- sort_validity(sorts)
head(as.data.frame(fit, include_interpretation = FALSE))
as.data.frame(fit, component = "scale_summary")
}
